import { LogColorDirective } from './log-color.directive';

describe('LogColorDirective', () => {
  it('should create an instance', () => {
    const directive = new LogColorDirective();
    expect(directive).toBeTruthy();
  });
});
